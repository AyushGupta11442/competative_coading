#include <iostream>

using namespace std;

int main() {
	int n;
	cin >> n;
	int total = 0;
	for (int i = 0; i < i * 3; i++) {
		int temp;
		cin >> temp;
		total += temp;
	
	}
	cout << (total == 0) ? "YES" : "NO";
}